# HackSquad — 게임 기획안

> 이 문서는 Claude Code 작업 시 게임 기획/구조 참고용으로 사용한다. 코드 작업 규칙은 `CLAUDE.md`를 따르고, 본 문서는 게임 기획·시스템 설계 내용을 다룬다.

## 1. 개요

- **게임명**: 핵쟁이스쿼드 (HackSquad)
- **장르**: 수집형 RPG + 디펜스 + 로그라이트
- **레퍼런스**: 콩콩던전
- **플랫폼**: 모바일 (Unity)

### 핵심 컨셉

히어로 4개를 등록해 스쿼드를 구성하고, 화면 중앙에 고정된 채로 사방에서 다가오는 몬스터를 자동으로 상대하는 오토배틀러형 디펜스 게임. 인게임 진행 중 레벨업으로 스킬을 강화하며 빌드를 만들어가고, 스테이지를 클리어해 나간다.

## 2. 핵심 게임플레이 루프

1. 스쿼드(히어로 4명)는 화면 중앙에 위치, 이동하지 않고 몬스터 방향으로 회전만 한다.
2. 각 히어로는 독립적으로 가장 가까운 몬스터를 타게팅하여 공격한다.
3. 몬스터는 화면 중앙 기준 일정 거리(n) 밖에서 생성되어 중앙으로 다가온다.
4. 몬스터 처치 시 스쿼드 공유 EXP 획득.
5. 레벨업 시, 스쿼드 4명의 스킬 전체 풀 중 랜덤 3개를 후보로 제시 → 유저가 1개 선택하여 업그레이드.
6. 스킬은 최대 3레벨까지 업그레이드 가능. 스킬이 3레벨인 상태에서 업그레이드 후보에 해당 스킬이 선택될 경우, 일반 업그레이드 대신 해당 스킬의 **최종형태**가 등장한다. 최종형태를 선택하면 그 스킬은 이후 업그레이드 풀에서 영구 제외된다.
7. 스테이지는 기본적으로 `웨이브 5분 → 중간보스 → 웨이브 5분 → 최종보스` 형식이며, 스테이지에 따라 가변적 구성도 존재한다(예: 보스 4연속).
8. 중간보스 처치 시 레벨업과 별개로 1~5개의 랜덤 스킬 업그레이드 보너스 지급.
9. 스테이지(런) 진입 시 레벨·스킬 진행 상태는 1부터 초기화된다. (인게임 한정 진행)

## 3. 히어로 시스템

### 공통 스탯

모든 히어로는 타입 구분(힐러/탱커 등) 없이 동일한 스탯 풀을 공유한다.

- 공격속도
- 공격력
- 치명타확률
- 치명타데미지
- 사거리
- 체력

> 히어로의 "역할"은 타입이 아니라 **등록된 스킬 구성**에서 나온다. 예: 힐 효과를 가진 스킬을 보유하면 그 히어로는 힐러처럼 기능한다.

### 등급

- 노말 / 레어 / 에픽 / 레전드 4단계
- 등급에 따라 성능 차이가 크게 발생
- 장비(무기/방어구/악세서리)도 동일한 4단계 등급 체계를 공유

### 성장

- 재화 + 가챠로 획득한 동일 히어로 카드 수를 소모해 레벨업
- 후반부터 무기/방어구/악세서리 장착 가능

## 4. 스킬 시스템

- 히어로별 스킬 슬롯은 **3개 또는 4개로 고정** (히어로마다 슬롯 수는 같을 수도, 다를 수도 있으나 최대 레벨 등 공통 규칙은 전 히어로 동일)
- 모든 스킬 업그레이드는 캐릭터 컨셉에 종속적 — 히어로 간 공통 업그레이드 요소는 없음
- 업그레이드는 레벨업 시 랜덤 3개 후보 중 선택하는 방식으로 누적 성장
- **스킬 최종형태**: 스킬이 3레벨(만렙)에 도달하면 다음 업그레이드 후보에 해당 스킬의 최종형태가 등장. 최종형태 선택 시 해당 스킬은 완성되어 이후 업그레이드 풀에서 제외됨 (스킬별로 독립 적용)

## 5. 어빌리티 시스템

- 모든 캐릭터/스쿼드에 공통 적용되는 영구 버프 (계정 단위)
- 재화로 뽑으며, 플레이어 레벨업마다 3개씩 누적 횟수 증가 (예: 레벨 10 → 누적 30회)
- 인게임 런 진행과 무관하게 영구 적용

## 6. 상점 / 가챠

- 패키지, 재화, 가챠(히어로 카드) 판매
- IAP 결제 포함 (검증 정책은 8장 참고)

## 7. 던전 시스템

- 골드던전, 재화던전 등 추후 확장 예정
- 데이터 구조: `DungeonData`(ScriptableObject) = `EntryConditionData`(입장 조건) + `RewardTableData`(보상 테이블) + `WaveCompositionData`(웨이브 구성)
- 신규 던전 타입 추가 시 코드 수정 없이 데이터 에셋 추가만으로 대응 가능하도록 설계

## 8. 스테이지 시스템

- 기본 포맷: `웨이브 5분 → 중간보스 → 웨이브 5분 → 최종보스`
- 단, 스테이지별로 가변적 (예: 보스만 4연속 등장)
- 데이터 구조: `StageData`(ScriptableObject) = `List<StagePhaseData>`
  - `StagePhaseData`: `PhaseType`(WaveSpawn / MidBoss / FinalBoss), 지속시간(웨이브 구간), 보스 데이터 참조(보스 구간), 보너스 지급 여부(중간보스)
- 포맷이 고정 형태가 아닌 페이즈 리스트이므로, 순서·구성을 자유롭게 조립 가능

## 9. 기술 아키텍처 방향

코드 품질/구조 규칙은 `CLAUDE.md`를 따르되, 게임 도메인 특화 설계 방향은 아래를 기준으로 한다.

### Hero

- 히어로 타입 분기가 없으므로 `Hero`는 **단일 클래스**(상속 불필요)로 설계
- 구성: 공통 스탯 + `SkillSlots`(`SkillBase[]`) + `EquipmentSlots`(`IEquipable[]`)
- 합성(composition) 중심 — 타입별 서브클래싱으로 인한 불필요한 추상화 지양

### Skill

- `SkillBase`(abstract): 레벨, 만렙, `Upgrade()` 공통 로직
- 효과 타입별 상속: `DamageSkill`, `HealSkill`, `BuffSkill`(`DebuffSkill` 등 확장 가능) — `ApplyEffect()` 오버라이드
- 오버로딩 활용: `ApplyEffect(Monster target)` (단일 타겟) / `ApplyEffect(Monster[] targets)` (광역)
- 최종형태 해금 조건은 `SkillBase`의 가상 메서드(`IsMaxLevel()`)로 판별. 3레벨 도달 시 업그레이드 풀에 `FinalForm` 후보가 등장하며, 선택 후 `IsFinalized` 플래그로 풀 제외 처리

### Equipment

- `IEquipable` 인터페이스: `EquipmentSlot Slot`, `ApplyStatBonus(Hero hero)` — 무기/방어구/악세서리가 공통 구현

### Grade 공통화

- `IGradable` 인터페이스: `Grade Grade`, `GetGradeMultiplier()` — `HeroData`, `EquipmentData`가 공통 구현하여 등급별 스탯 배율 로직 재사용

### Stage / Dungeon

- 데이터 합성 구조 (7, 8장 참고) — 런타임 로직은 데이터를 읽기만 하며, 신규 콘텐츠 추가 시 코드 수정 최소화

### 최적화

- Addressables로 리소스 분리 로딩/메모리 관리 (대규모 프로젝트 대비)
- 오브젝트 풀링, GC 할당 최소화
- DrawCall 최소화 (Sprite Atlas, GPU Instancing)

## 10. 백엔드 (Firebase)

- DB, 로그인/인증 등 모든 서버 작업은 Firebase 사용
- 재화·가챠 등 조작 가능성이 있는 핵심 로직은 클라이언트 검증이 아닌 **서버(Firebase Functions) 검증**으로 처리, 클라이언트 데이터는 신뢰하지 않음
- 인앱결제(IAP) 영수증은 서버에서 Google Play / App Store 영수증 검증 후 재화 지급
- App Check: 비정상/변조 클라이언트 API 호출 차단
- Remote Config: 밸런스/이벤트/강제 업데이트 플래그 실시간 조정
- Crashlytics: 크래시 리포트
- Analytics: 유저 행동/리텐션 분석
- Cloud Messaging: 푸시 알림
- 서버 로그에 재화 지급/가챠 결과 기록, 이상 패턴 탐지 가능하도록 설계

## 11. 수익화

- 광고: AdMob
- 인앱결제(IAP): 서버 검증 필수 (10장 참고)

## 12. 미정 사항 (TBD)

- 스킬 최대 레벨 수치 (4 또는 5 후보)
- 히어로/스킬/장비 등급별 정확한 스탯 배율
- 던전 추가 타입 (이벤트/길드 등) 범위
- 스킬트리 슬롯별 정확한 업그레이드 분기 수
